# Copy to .env and set your own values. .env is gitignored and never committed.
POSTGRES_USER=solar
POSTGRES_PASSWORD=change_me
POSTGRES_DB=solar_forecast

# Host port the Postgres container is published on. Defaults to 5433 to avoid
# clashing with a PostgreSQL already installed on the host machine (5432).
POSTGRES_PORT=5433
