# Copy this to .env and fill in your own values — .env itself is never committed.
POSTGRES_USER=solar
POSTGRES_PASSWORD=change_me
POSTGRES_DB=solar_forecast

# Host port Postgres is published on. 5433 keeps it clear of a local install on 5432.
POSTGRES_PORT=5433
